diff -Naurp elfutils-0.148_org/src/strip.c elfutils-0.148/src/strip.c
--- elfutils-0.148_org/src/strip.c	2014-02-27 11:27:49.545331187 +0530
+++ elfutils-0.148/src/strip.c	2014-02-27 11:33:20.533336552 +0530
@@ -1673,6 +1673,11 @@ handle_elf (int fd, Elf *elf, const char
     INTERNAL_ERROR (fname);
 
   memcpy (newehdr->e_ident, ehdr->e_ident, EI_NIDENT);
+  /* Make contents of e_ident[EI_ABIVERSION] and above same with 
+   * the contents generated by strip/objcopy commands */
+#ifdef __mips__
+  memset (&(newehdr->e_ident[EI_ABIVERSION]), 0, EI_NIDENT - EI_ABIVERSION);
+#endif /* __mips__ */
   newehdr->e_type = ehdr->e_type;
   newehdr->e_machine = ehdr->e_machine;
   newehdr->e_version = ehdr->e_version;
