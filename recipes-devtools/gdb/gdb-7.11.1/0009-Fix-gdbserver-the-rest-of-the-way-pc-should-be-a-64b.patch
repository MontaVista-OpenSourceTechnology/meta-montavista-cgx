From df567c42f3158c1e1192e081c85807628bb85aa7 Mon Sep 17 00:00:00 2001
From: Andrew Pinski <apinski@cavium.com>
Date: Mon, 10 Oct 2016 18:49:47 -0700
Subject: [PATCH 9/9] Fix gdbserver the rest of the way, pc should be a 64bit
 int.

Signed-off-by: Andrew Pinski <apinski@cavium.com>
---
 gdb/gdbserver/linux-aarch64-low.c | 4 ++--
 1 file changed, 2 insertions(+), 2 deletions(-)

diff --git a/gdb/gdbserver/linux-aarch64-low.c b/gdb/gdbserver/linux-aarch64-low.c
index 862b807..28c312b 100644
--- a/gdb/gdbserver/linux-aarch64-low.c
+++ b/gdb/gdbserver/linux-aarch64-low.c
@@ -166,7 +166,7 @@ aarch64_get_pc (struct regcache *regcache)
 {
   if (register_size (regcache->tdesc, 0) == 8)
     {
-      unsigned long pc;
+      unsigned long long pc;
 
       collect_register_by_name (regcache, "pc", &pc);
       if (debug_threads)
@@ -191,7 +191,7 @@ aarch64_set_pc (struct regcache *regcache, CORE_ADDR pc)
 {
   if (register_size (regcache->tdesc, 0) == 8)
     {
-      unsigned long newpc = pc;
+      unsigned long long newpc = pc;
       supply_register_by_name (regcache, "pc", &newpc);
     }
   else
-- 
2.6.4

