diff -Naurp webkitgtk-2.8.5_org/Source/WTF/wtf/Platform.h webkitgtk-2.8.5/Source/WTF/wtf/Platform.h
--- webkitgtk-2.8.5_org/Source/WTF/wtf/Platform.h	2015-12-07 23:41:18.958818278 -0800
+++ webkitgtk-2.8.5/Source/WTF/wtf/Platform.h	2015-12-07 23:58:53.438720941 -0800
@@ -344,6 +344,15 @@
 #define WTF_CPU_NEEDS_ALIGNED_ACCESS 1
 #endif
 
+/* CPU(AARCH64) - Aarch64 */
+#if defined(__aarch64__)
+#define WTF_CPU_AARCH64 1
+/* 32-bit mode on AARCH64 */
+#if !defined(__LP64__)
+#define WTF_CPU_AARCH64_32 1
+#endif
+#endif
+
 /* ==== OS() - underlying operating system; only to be used for mandated low-level services like 
    virtual memory, not to choose a GUI toolkit ==== */
 
@@ -653,7 +662,9 @@
     || CPU(S390X) \
     || CPU(MIPS64) \
     || CPU(PPC64) \
-    || CPU(PPC64LE)
+    || CPU(PPC64LE) \
+    || CPU(AARCH64) && !CPU(AARCH64_32)
+
 #define WTF_USE_JSVALUE64 1
 #else
 #define WTF_USE_JSVALUE32_64 1
